<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<input type="checkbox" id="task" name="task_completed" <%= 'checked' if @task_completed %>> This task is completed

<%= link_to 'Edit', task_edit_path %>

<br>

<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>

<br>

<%= link_to 'Back to task list', tasks_path %>
