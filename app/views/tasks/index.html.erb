<h1>My tasks</h1>

<% @tasks.each do |task| %>
  <div>
    <input type="checkbox" id="task" name="task"> <%= link_to task.title, task_path(task) %>
  </div>
<% end %>

<%= link_to 'Add a task', tasks_new_path %>
